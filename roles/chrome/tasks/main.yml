---
- name: Check for npx
  ansible.builtin.command: npx --version
  register: chrome_npx_check
  changed_when: false
  failed_when: false

- name: Assert Node.js is available
  ansible.builtin.assert:
    that: chrome_npx_check.rc == 0
    fail_msg: chrome requires Node.js (npx); apply the node role before this role

- name: Remove system chrome
  ansible.builtin.include_tasks: remove-system-chrome.yml
  when: chrome_remove_system_chrome

- name: Install chrome system dependencies
  ansible.builtin.apt:
    pkg: "{{ chrome_system_dependencies + chrome_release_dependencies[ansible_facts['distribution_release']] }}"
    state: present
    update_cache: true
    cache_valid_time: 3600

- name: Check current chrome version
  ansible.builtin.command: /usr/local/bin/google-chrome --version
  register: chrome_current_version
  changed_when: false
  failed_when: false

- name: Resolve the chrome channel version
  ansible.builtin.uri:
    url: https://googlechromelabs.github.io/chrome-for-testing/last-known-good-versions.json
    return_content: true
  register: chrome_channel_versions
  when: chrome_version | string in chrome_release_channels

- name: Set the chrome target version
  ansible.builtin.set_fact:
    chrome_target_version: >-
      {{ chrome_channel_versions.json.channels[chrome_version | capitalize].version
         if chrome_version | string in chrome_release_channels else chrome_version | string }}

- name: Install chrome
  ansible.builtin.command: "npx --yes @puppeteer/browsers install chrome@{{ chrome_target_version }} --path /opt"
  register: chrome_install_result
  changed_when: true
  when: chrome_current_version.rc != 0 or chrome_target_version not in chrome_current_version.stdout

- name: Process chrome path
  ansible.builtin.set_fact:
    chrome_path: "{{ chrome_install_result.stdout | regex_search('chrome@[\\d.]+ (.+)', '\\1') | first }}"
  when: chrome_install_result is not skipped

- name: Link chrome
  ansible.builtin.file:
    src: "{{ chrome_path }}"
    dest: /usr/local/bin/google-chrome
    state: link
    force: true
  when: chrome_install_result is not skipped

- name: Chromedriver tasks
  ansible.builtin.import_tasks: chromedriver.yml
  when: chrome_install_chromedriver
